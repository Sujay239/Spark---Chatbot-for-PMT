import test from 'node:test';import assert from 'node:assert/strict';import { generateTests } from '../src/test-generation/generator.js';
const qa=Array.from({length:30},(_,i)=>({id:`QA-${i}`,question:`How do I use TENS feature number ${i}?`,answer:`Answer ${i}`,sourceRef:'qa.csv'}));
const spec={qa};const config={testing:{questionsPerCycle:40,similarityThreshold:.85,enableRegressionTests:false,enableNegativeTests:true,enableHallucinationTests:true,enablePromptInjectionTests:true,enableEdgeCases:true,enableMultiTurnTests:true,regressionShare:.2,categoryWeights:{DIRECT:.1,PARAPHRASE:.2,SEMANTIC:.1,TYPO:.1,INFORMAL:.1,MULTI_INTENT:.05,CONTEXTUAL:.05,AMBIGUOUS:.05,NEGATIVE:.05,OUT_OF_SCOPE:.03,HALLUCINATION:.03,BOUNDARY:.03,PROMPT_INJECTION:.03,SECURITY:.02,LONG_CONTEXT:.02,MULTILINGUAL:.01}}};
test('generator returns diverse non-copy questions',async()=>{const tests=await generateTests(spec,config);assert.ok(tests.length>=35);assert.ok(new Set(tests.map(x=>x.category)).size>=8);assert.equal(tests.filter(t=>t.source&&t.question.toLowerCase()===t.source.question.toLowerCase()).length,0);});
