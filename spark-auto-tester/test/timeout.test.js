import test from 'node:test';import assert from 'node:assert/strict';import http from 'node:http';import { WebhookClient } from '../src/webhook/client.js';
test('webhook client returns timeout without crashing run',async()=>{const server=http.createServer(()=>{});await new Promise(r=>server.listen(0,'127.0.0.1',r));const port=server.address().port;const cfg={target:{url:`http://127.0.0.1:${port}/hook`,method:'GET',headers:{},request:{location:'query',questionField:'chatInput',sessionField:'sessionId',staticFields:{}},response:{answerPaths:['text'],sourcePaths:[],intentPaths:[],confidencePaths:[]}},execution:{requestTimeoutMs:25,maxRetries:0,backoffBaseMs:1,retryStatusCodes:[503]}};try{const x=await new WebhookClient(cfg).send('hello','s1');assert.match(x.error,/TIMEOUT/);assert.equal(x.httpStatus,null);}finally{server.closeAllConnections?.();server.close();}});
